// https://codeforces.com/contest/1760/problem/A
#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, a, b, c, mx, mi; cin >> n;
    while(n--){
        cin >> a >> b >> c;
        mx = max({a, b, c});
        mi = max({a, b, c});
    }
}
